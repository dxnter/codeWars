const solution = str => {
    let strNum = parseInt(str);
    let max = 0;
    for (let i = 0; i < strNum.length; i++) {
        let testMax = 
    }
};

console.log(solution('5654396228')); fuck this algorithm
